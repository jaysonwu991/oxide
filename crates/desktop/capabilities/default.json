{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the oxide desktop window (events for streaming agent output).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
