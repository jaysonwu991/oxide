---
description: Review the current diff with the rust-reviewer subagent.
agent: rust-reviewer
subtask: true
---

Review the current working-tree changes for the `oxide` project.

Use `git diff` (and `git diff --staged` when relevant) to gather the changes,
then apply your review rubric. Scope or focus: $ARGUMENTS
